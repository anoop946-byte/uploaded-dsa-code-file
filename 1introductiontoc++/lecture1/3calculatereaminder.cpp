#include<iostream>
using namespace std;
int main(){
    int a,b;
    cout<<"enter first and second number:";
    cin>>a;
    cin>>b;
    // int d=a/b;
    // int reaminder=a-b*d;
    // cout<<"the required remainder is "<<reaminder;
    int r=a%b;
    cout<<"the requried reaminder is "<<r;
    
}
